<script type="text/javascript">
  window.staticImages = {
    epicruxLogo: "<%= asset_path('epicrux-logo.png') %>",
  }
  
  <% if logged_in? %>
    window.currentUser = <%= render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %>
  <% end %>
</script>

<div id="root"></div>
